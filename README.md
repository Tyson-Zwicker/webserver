# webserver
This started as a simple multithreaded Node webserver...

Then I actually used it for a project and left it on an exposed port-forwarded surface...

Now its morphing into a fun side-project to just mess around with script kiddies.  The logs they leave behind are
full of information you can use to waste their time.

